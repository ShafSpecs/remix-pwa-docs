import GeneralError, { type GeneralErrorProps } from "~/components/GeneralError";

const PwaSlugErorr = (props: GeneralErrorProps) => {
  return <GeneralError {...props} />;
};

export default PwaSlugErorr;
